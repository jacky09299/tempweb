name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ] # 如果您的主分支叫 master，請把這裡改成 master

permissions:
  contents: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v4

      - name: Setup Node.js ⚙️
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Install Dependencies 🔧
        run: npm install

      - name: Build 🏗️
        run: npm run build
        # 這裡會建立 dist 資料夾 (Vite 的預設打包路徑)

      - name: Deploy 🚀
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: dist # 這是 Vite 打包後生成的資料夾名稱
